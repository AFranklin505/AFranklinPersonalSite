@inject IJSRuntime JS
<div id="linkedIn" class="badge-base LI-profile-badge" data-locale="en_US" data-size="small" data-theme="dark" data-type="HORIZONTAL" data-vanity="anthonyfranklin520" data-version="v1"></div>
@code {
    protected override async Task OnInitializedAsync()
    {

        await JS.InvokeAsync<IJSObjectReference>("import", "./Shared/LinkedInComponent.razor.js");
        await base.OnInitializedAsync();
    }
}
